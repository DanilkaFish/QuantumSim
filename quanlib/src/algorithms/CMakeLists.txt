project(grover)
add_library(${PROJECT_NAME} OBJECT grover.cpp)
add_library(sub::${PROJECT_NAME} ALIAS ${PROJECT_NAME})
# add_test()
target_include_directories(${PROJECT_NAME} PRIVATE ${QC_SOURCE_PATH})
